const MetricModel = require('../models/metric-model');

const getMetricData = (req,res)=>{
    try{
        
        
    }catch(err){

    }
}

const addMetricData = (req,res)=>{
    try{

    }catch(err){

    }
}

module.exports = {
    getMetricData,
    addMetricData
}